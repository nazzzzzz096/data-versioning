1.create git repo and clone it in local
2.create code.py and add code to it (it will save a csv file to a new data folder)
3.Do a git add-commit-push  before initializing DVC
4.now we do "dvc init" ( when we do it , creates .dvc and .dvcignore)
5. now do "mkdir s3"(creates a new s# directory)
6.now we do "dvc remote add -d myremote s3"
7.next "dvc add data/"( now it ask to do  "git rm -r --cached 'data'"
            "git commit -m "stop tracking data"" do both steps and rerun "dvc add data/")
( it says that data in data folder is responsible for versioning)
    ### we are doing it becasue first git handled the data now we are transfering it to dvc to handle.
8.rerun "dvc add data/"it created data.dvc which has the id about dvc and git communication)
    ( it says that data in data folder is responsible for versioning)

9. then "dvc commit" and "dvc push"
10.do a git add-commit- and git push for first version of data
11. now we add new row in into mycode.py to append a new row and check "dvc status"
12. now we do "dvc commit" and "dvc push" (it create new files with updated row and a new id also creted for it)
